HYPER-SILLs-By-WelshDog.zip
.env
logs/
__pycache__/
*.pyc
.venv/
output/
dist/

# Ship the prebuilt dense index so the deployed MCP serves real embeddings
# instead of rebuilding (and silently TF-IDF'ing) on boot — but never the
# local backup snapshots.
vector-store/*
!vector-store/skill_index.json
*.bak
